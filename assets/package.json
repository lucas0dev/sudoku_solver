{
  "name": "assets",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "cd .. && mix assets.deploy && rm -f _build/esbuild"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "deploy": "^1.0.3"
  }
}
